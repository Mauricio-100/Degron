#!/usr/bin/env node

/**
 * Point d'entrée pour la commande 'drn' en ligne de commande.
 * Ce script est exécuté chaque fois qu'un utilisateur tape 'drn' dans son terminal.
 * Son seul rôle est d'importer et d'exécuter le fichier principal du client CLI.
 */

import '../cli.js';
